if [ -d /opt/homebrew ]; then
	eval $(/opt/homebrew/bin/brew shellenv)
fi

if [ -d ${HOME}/.profile.d ]; then
	if [ "$(ls -A ${HOME}/.profile.d)" ]; then
		for file in ${HOME}/.profile.d; do
			source ${file}
		done
	fi
fi

export PATH="/Users/john/Library/Python/3.8/bin:${PATH}"

